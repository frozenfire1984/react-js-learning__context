//import {createContext} from 'react';

export const themes = {
    light: {
        background: 'white',
        textColor: 'black',
    },
    dark: {
        background: 'black',
        textColor: 'white',
    },
};

//export const ThemeContext = createContext();
/*export const ThemeContext = createContext({
    theme: themes.light,
    //toggleTheme: () => {},
});*/


